LDRC2SIM — WiFi config portal + web OTA (2026-06-06).
STATUS: CONFIRMED WORKING on hardware (2026-06-06) — flashed over USB, flies in
        the sim, WiFi portal + OTA verified. This is a known-good restore point.

Adds (on top of the confirmed-flying LDRC2SIM_final):
- src/web_portal.{h,cpp}: WIFI_AP_STA. Open AP "LDRC2SIM" always up for setup;
  joins home WiFi in background once creds saved (NVS/Preferences).
- Web UI on :80 at http://LDRC2SIM.local (mDNS) or http://192.168.4.1 (AP):
  Status (live RC) / Wi-Fi config + scan + forget / Help / OTA upload.
- Captive portal (DNS catch-all) in AP mode.
- Web OTA via Update.h.
- platformio.ini: board_build.partitions = default_8MB.csv (dual app slot for OTA).
  -> THIS build must be flashed over USB once (partition layout changed).
- USB HID joystick + RC decode UNCHANGED from LDRC2SIM_final (VID 0x1209/PID 0x525C,
  AXIS_SOURCE {6,7,0,1,3,2,4,5}, rudder reversed, RX2SIM order).
- LDRC2SIM_wifi_firmware.bin = built image (use OTA or esptool full flash).
