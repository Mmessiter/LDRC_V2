LDRC2SIM backup — 2026-06-07 (evening)
Version: LDRC2SIM-1.0.12
Git:     LDRC2SIM @ wifi-portal, commit b11c484

WHAT TODAY ADDED:
- v1.0.11: OTA updates now also come from messiter.com (public mirror), the same
  way the RXV2 / ReedsV2 projects publish. Build the website tree + manifest from
  dev/*.bin with dev/stage_website.py, then FTP-mirror it with dev/publish_website.sh.
  web_portal.cpp FW_PUBLIC_MANIFEST -> https://messiter.com/ldrc2sim/release/manifest.json
  Live and verified (manifest + bins serve 200 with Content-Length).
- v1.0.12: animated progress bar on the Firmware page during an OTA install.

Live on the dongle at LDRC2SIM.local. The local dev firmware server
(dev/firmware_server.py, port 8001) still works alongside messiter.com.
FTP credentials are in the gitignored dev/ftp_credentials.sh (NOT in this backup).

This snapshot = src/ + platformio.ini + README.md + the v1.0.12 firmware.bin.
