# PlatformIO build output
.pio/

# Editor / IDE
.vscode/

# Note: backup/ (including *.bin restore points) is intentionally tracked.

# Reference copies of other projects (not part of LDRC2SIM)
RXV2 copy/
ReedMachine copy/

# messiter.com FTP password used by dev/publish_website.sh — never commit
dev/ftp_credentials.sh
