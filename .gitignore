# ── Build output / IDE / OS cruft — regeneratable, never tracked ──
# (patterns without a leading slash match at any depth, so they cover every subproject)
.pio/
.cache/
.vscode/
compile_commands.json
.DS_Store

# ── Secrets — NEVER commit ──
# The messiter.com FTP password lives here (also ignored inside LDRC2SIM/.gitignore).
ftp_credentials.sh

# ── Android app (RXV2App) — build artifacts + publish staging ──
RXV2App/android/RXV2App/.gradle/
RXV2App/android/RXV2App/app/build/
RXV2App/android/RXV2App/local.properties
RXV2App/android/site/
RXV2App/android/RXV2App/build/

# App Store screenshots: regenerate with RXV2App/appstore/shoot.sh
RXV2App/appstore/screenshots/
